# pytest.ini
[pytest]
minversion = 7.0
testpaths = tests
python_files = *_test.py test_*.py
addopts = -ra --order-scope=session

markers =
    e2e: end-to-end tests that hit a real 1Password vault (requires OP_ACCOUNT and 'op' CLI)
    order: control test execution order
